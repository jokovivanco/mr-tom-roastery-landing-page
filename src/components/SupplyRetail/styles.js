import { makeStyles } from '@material-ui/core'

export default makeStyles((theme) => ({
  section: {
    backgroundColor: 'transparent'
  },
  horizontal: {
    whiteSpace: 'nowrap',
    overflowX: 'scroll'
  }
}))